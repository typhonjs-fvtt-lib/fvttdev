Today
-----------------------------------------
1. Flag verification; especially
  - shorthand 'char' overlap
  - flag parsing for matched "<XXX>=<XXX>"

2. Clean up package.json imports IE latest Mocha / define peer dependencies / update license to correct MPL attribution

2. Examine esmbundler and entry points / initial code handling for JS / TS

3. TODO: consider `cwd` flag and implications

4. Parse module.json / system.json

